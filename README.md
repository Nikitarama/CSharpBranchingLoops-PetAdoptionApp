A pet adoption application that displays all pets, 
adds pets with an ID, 
add data to existing or added pets while the app is running instead of hardcoded in the code,
a donation suggestion has been added for each pet already in the system,
added a search to display pets with specific descriptions
